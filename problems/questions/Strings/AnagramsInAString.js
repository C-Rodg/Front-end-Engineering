// Given 2 strings 's' and 't', find and return all indexes in a string s where t is an anagram.

// Input: ('acdbacdacb', 'abc')
// Output: [3, 7]

// Hint 1: Hash map

// Hint 2: sliding window

// Hint 3: hash map values become positive when being removed, negative when being added
