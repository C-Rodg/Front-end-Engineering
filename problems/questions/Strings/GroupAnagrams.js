// Given an array of strings, return back the grouped words that are anagrams.

// Input: ['abc', 'bcd', 'cba', 'cbd', 'efg']
// Output: [['abc', 'cba'], ['bcd', 'dbc'], ['efg']]

// Anagram - a word formed by rearranging the letters of another

// Brute Force:
// Sort the characters of the string.  Set the sorted value as the key and the value as the original value.
// Return the values of the hashmap.

// Hint 1:
// Use portions of brute force technique. But how can we remove the sort?

// Hint 2:
// Use an array as our keys...

// Hint 3:
// Array should think about having 26 slots...
