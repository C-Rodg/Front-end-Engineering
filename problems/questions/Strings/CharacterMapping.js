// Given two strings, determine if
// 's1' can be properly mapped to 's2'.

// Input: 'abc' 'def'
// Output: true because a->d, b->e, c->f

// Input: 'aca' 'def'
// Output: false because a->d, c->e, but then a is already mapped to 'd'

function validMapping(string1, string2) {
	return Boolean;
}

// Hint 1: Maps
