// Given a string of parentheses,
// find the minimum number of ')' or '(' we must add
// to make the resulting parentheses valid.

function minimumParenthesesToAdd(str) {
	return Number;
}

// Hint 1:  counters
