// Given an array of integers and a target sum 'k',
// return the subarray that sums up to 'k'.

// Input: [1, 3, 2, 5, 7, 2] and k = 14
// Output: [2, 5, 7] because this subarray equals 14

// Hint 1: Could probably be done with left & right pointers, but a
// - better method might be to use map..

// Hint 2: The hash map should be initialized with a certain value incase
// - the subarray starts at index 0...
