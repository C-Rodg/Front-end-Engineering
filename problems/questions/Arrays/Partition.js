// Given an array and a partition value,
// rearrange the array so that everything less than
// the partition value is on the left and
// everything more is on the right.

// Attempt to do this without any extra space.

function partition(arr, n) {
	return arr;
}

// Input: [8,9,2,4,1,0], and n = 3
// Output: [0,2,1,4,9,8]
