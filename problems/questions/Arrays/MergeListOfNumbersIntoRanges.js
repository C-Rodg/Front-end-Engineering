// For an array of sorted numbers (low -> high),
// return an array of arrays of number ranges.

// Input: [0, 1, 2, 5, 7, 8, 9, 9, 10, 11, 15]
// Output: [[0,2], [5], [7,11], [15]]

// Hint 1: Track low and high

// Hint 2: For a current index, how can we determine if it breaks the range?
