// Given an array of strings, determine if a circle of chained words can be created.
// This means the last character for one string must match up with the first character on another and form a loop.

// Input: ['easter', 'trunk', 'rabbit', 'kee' ]
// Output: true : easter -> rabbit -> trunk -> kee -> easter

// Hint 1: Graph cycles

// Hint 2: DFS

// Hint 3: Create a matrix

// Hint 4: Tracking current word, start word, remaining, visited
