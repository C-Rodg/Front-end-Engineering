// Given an array of integers and operands,
// calculate the results.
// The integers are provided in the reverse polish notation format.
// This means for every operand you see, you take the two previous
// integers and use it.

// Input: [1, 2, 3, +, 2, *, -]
// Output: -9 because 1 - (2 + 3) * 2

function reversePolish(arr) {
	return Number;
}

// Hint 1: Stack
