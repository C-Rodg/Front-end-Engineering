// Given two rectangles, find the area of intersection

function Rectangle(minX, minY, maxX, maxY) {
	this.minX = minX;
	this.minY = minY;
	this.maxX = maxX;
	this.maxY = maxY;
}

// Hint 1: Do we need to calculate both x & y at the same time or no?
