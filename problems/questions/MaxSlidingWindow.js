// Difficulty: Hard

// Given a large array of integers and a window of size w,
// find current maximum in the window as the window
// slides through the entire array.

// Input: [2, -1, 5, 0, 8], window size = 3
// Output: [5, 5, 8]

// Runtime: O(n)
// Memory: O(w) where 'w' is the window width

function findSlidingMaxWindow(arr, windowSize) {
	// TODO:
}
